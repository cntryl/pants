name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: ${{ matrix.os }} / Release
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install .NET 10 SDK
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Restore locked dependencies
        run: dotnet restore Pants.slnx --locked-mode

      - name: Verify formatting and analyzers
        run: dotnet format Pants.slnx --verify-no-changes --no-restore

      - name: Build
        run: dotnet build Pants.slnx --configuration Release --no-restore

      - name: Test crash recovery
        run: >-
          dotnet test Pants.slnx
          --configuration Release
          --no-build
          --filter "FullyQualifiedName~PantsCloudCrashRecoveryTests"
          --logger "console;verbosity=detailed"
          --blame-hang-timeout 2m
          --blame-hang-dump-type none

      - name: Test remaining suite
        run: >-
          dotnet test Pants.slnx
          --configuration Release
          --no-build
          --filter "FullyQualifiedName!~PantsCloudCrashRecoveryTests"
          --logger "trx;LogFileName=remaining.trx"
          --results-directory TestResults
          --blame-hang-timeout 3m
          --blame-hang-dump-type none

      - name: Report slowest tests
        if: ${{ always() }}
        shell: pwsh
        run: |
          $resultPath = Join-Path $PWD "TestResults/remaining.trx"
          if (-not (Test-Path $resultPath)) {
            Write-Warning "No remaining-suite TRX result was produced."
            exit 0
          }

          $results = Select-Xml `
            -Path $resultPath `
            -XPath "//*[local-name()='UnitTestResult']" |
            ForEach-Object { $_.Node }
          $results |
            Sort-Object { [TimeSpan]::Parse($_.duration) } -Descending |
            Select-Object -First 25 |
            ForEach-Object {
              Write-Host ("{0,12}  {1,-10}  {2}" -f $_.duration, $_.outcome, $_.testName)
            }
